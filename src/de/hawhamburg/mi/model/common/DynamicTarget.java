package model.common;

/**
 * A DynamicTarget has no representation in space and is only available during a predefined time.
 * It has a capacity defining the number of agents that can enter the target.
 * Such a target can represent a lecture.
 * DynamicTargets can only be drawn a defined time before the start-time and are only accessible 
 * during start- and end-time.
 * An agent can only enter a DynamicTarget if the number of agents is <= capacity. 
 * 
 * @author pascal
 *
 */
public class DynamicTarget {

}
